const initialState = {
	loading: false,
	currentUser: null,
	error: null
};

const userReducer = (state, action) => {
	switch (action.type) {
		default:
			return state;
	}
}

export default userReducer;